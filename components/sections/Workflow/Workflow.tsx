'use client';

/* ============================================================
   WORKFLOW — section orchestrator
   Renders the primary StickyWorkflow component.
   Mounted in the home scroll just before Contact.
   ============================================================ */

import { StickyWorkflow } from './StickyWorkflow';

export function Workflow() {
  return <StickyWorkflow />;
}

